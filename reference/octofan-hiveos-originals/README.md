# Octofan HiveOS Originals

Original Octominer/HiveOS files kept for reference while the new containerized controller is developed.

These files are not meant to be run from the repository root. The Docker image only copies `fan_controller_cli` from this directory because it remains the hardware interface for v1.
